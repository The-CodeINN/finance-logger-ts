const character = 'mario';

console.log(character);

// const inputs = document.querySelectorAll('input');

// inputs.forEach((input) => {
//   console.log(input);
// });

const cirum = (diameter: number) => {
  return diameter * Math.PI;
}
console.log(cirum(7.5).toFixed(2));